{
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "extensions": [
    // Other extensions or configurations you might have
  ],
  "runArgs": [
    // Other run arguments you might have
  ],
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y mysql-server",
  "forwardPorts": [
    // Ports you might want to forward
  ],
  "features": {
    // Other features you might have
  }
}
